{
  "name": "<%= underscore.slugify(moduleName) %>",
  "version": "0.1.0",
  "description": "<%= description %>",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "coverage": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec"
  },
  "engines": {
    "node": ">=0.10.0"
  },<% if(cli) { %>
  "bin": {
    "<%= underscore.slugify(moduleName) %>": "cli.js"
  },<% } %>
  "files": [
    "index.js"<% if(cli) { %>,
    "cli.js"<% } %>
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/<%= githubUsername %>/<%= underscore.slugify(moduleName) %>"
  },
  "author": "<%= name %> <<%= email %>> (<%= website %>)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/<%= githubUsername %>/<%= underscore.slugify(moduleName) %>/issues"
  },
  "homepage": "https://github.com/<%= githubUsername %>/<%= underscore.slugify(moduleName) %>",
  "dependencies": {},
  "devDependencies": {
    "coveralls": "2.11.2",
    "istanbul": "0.3.14",
    "mocha": "2.2.5",
    "mocha-lcov-reporter": "0.0.2"
  }
}
